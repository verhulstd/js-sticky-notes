{
  "name": "js-sticky-notes",
  "version": "1.0.4",
  "description": "A vanilla JS customisable sticky-note plugin",
  "main": "./dist/index.js",
  "scripts": {
    "css": "parcel build ./css/style.css --out-dir ./dist --out-file index --public-url ./",
    "js": "parcel build ./js/index.js --out-dir ./dist --out-file index --public-url ./ --global StickyNotes",
    "demo": "parcel ./demo/js/index.js --out-dir ./demo/js --out-file bundle --public-url ./ --no-hmr"
  },
  "keywords": [
    "post-it",
    "sticky notes",
    "sticky-notes",
    "vanilla-js-sticky-note",
    "color-notes"
  ],
  "author": "David Verhulst",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "autoprefixer": "^7.2.5",
    "babel-core": "^6.26.3",
    "babel-preset-env": "^1.6.1",
    "node-sass": "^4.7.2",
    "postcss-modules": "^1.1.0"
  }
}
